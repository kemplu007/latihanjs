// type data primitif

// string
const name = 'mas kemplu';

// number
const age = 25;

// boolean 
const hasKid = false;

// Null
const car = null;
 
// undifined
let test;

// symbol
const sym = Symbol();

// type data referensi

// string
const hobbies = ['musik', 'makan'];
console.log(hobbies[1]);
// object
const address = {
    city: 'magelang',
    bupati: 'ipin2'
};
console.log(address.bupati);